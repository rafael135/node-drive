import { User } from "../../types/Auth";

type NavbarProps = {
    user?: User;
};

const Navbar = ({ user }: NavbarProps) => {
    

    return (
        <header>

        </header>
    );
}

export default Navbar;